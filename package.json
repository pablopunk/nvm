{
  "name": "nvm",
  "version": "0.1.13",
  "description": "Nevermind: an AI-native command palette for your desktop",
  "author": "Pablo Varela",
  "license": "GPL-3.0-only",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "node ./scripts/prepare-dev.cjs && electron-vite dev --watch",
    "build": "electron-vite build",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "node scripts/check-design-system.cjs && pnpm typecheck",
    "palette:debug": "node scripts/palette-debug.cjs",
    "logs:tail": "node scripts/tail-log.cjs",
    "electron": "electron .",
    "electron:dev": "node ./scripts/prepare-dev.cjs && electron-vite dev --watch",
    "dist": "pnpm run dist:mac:arm64",
    "dist:mac": "pnpm run dist:mac:arm64",
    "dist:mac:arm64": "pnpm build && electron-builder --config electron-builder.yml --mac zip --arm64 --publish never",
    "dist:mac:x64": "pnpm build && electron-builder --config electron-builder.yml --mac zip --x64 --publish never",
    "dist:linux": "pnpm run dist:linux:x64",
    "dist:linux:x64": "pnpm build && electron-builder --config electron-builder.yml --linux AppImage deb --x64 --publish never",
    "dist:linux:arm64": "pnpm build && electron-builder --config electron-builder.yml --linux AppImage deb --arm64 --publish never",
    "dist:win:x64": "pnpm build && electron-builder --config electron-builder.yml --win nsis portable --x64 --publish never"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.74.0",
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "@vitejs/plugin-react": "latest",
    "cmdk": "^1.1.1",
    "debug": "^4.4.3",
    "electron-log": "^5.4.4",
    "electron-updater": "^6.3.9",
    "file-icon": "^6.0.0",
    "lucide-react": "latest",
    "ms": "^2.1.3",
    "react": "latest",
    "react-dom": "latest",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "concurrently": "latest",
    "electron": "^41.5.0",
    "electron-builder": "^26.8.1",
    "electron-vite": "^5.0.0",
    "wait-on": "latest"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "electron-winstaller",
      "esbuild"
    ]
  }
}
